services:
  minecraft:
    container_name: minecraft-server
    image: itzg/minecraft-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      VERSION: latest
      ONLINE_MODE: "TRUE"
    volumes:
      - ./minecraft/data:/data
    restart: unless-stopped
